# Run report — 20260718-203027-487673

> **วันที่:** 2026-07-18
> **Scope:** model-downsize (DEF-001 fix → L0+L1 regression; L2 attempted under wall-time cap)
> **Level:** 1 primary (`20260718-203027-487673`); L2 attempt `20260718-203307-07344c` (partial / stopped)
> **Environment label:** mixed (local services up; Fabric capacity not active; Ollama generate OK on `qwen2.5-coder:3b`)
> **Raw evidence:** `data/local/qa/loop-engineering/runs/20260718-203027-487673/` (gitignored)

## Summary

| Metric | Value |
|--------|-------|
| L0 env smoke | **pass** — SCN-ENV-001 + SCN-ENV-002 (generate ok for `qwen2.5-coder:3b`) |
| L1 pytest | **pass** (**390**/390) |
| L1 conformance | **pass** (**11**/11) — ไม่ได้ weaken |
| L2 live / golden | **partial / not-run** — started Level 2 + IncludeGolden; SCN-CHAT-001 hung past ~10–12 min wall on CPU; stopped; no summary.json for L2 run |
| Repair rounds used | **2** / 3 (round 1: unload+retry 7b OOM; round 2: switch to 3b) |

## Fix applied (config only)

- Pulled `qwen2.5-coder:3b`; `/api/generate` smoke (`hi`, `num_predict=8`) → OK (~9s)
- `.env`: `OLLAMA_MODEL=qwen2.5-coder:3b` (other keys untouched; analyst/BA empty left empty)
- Restarted project Backend/Frontend on ports 8000/8501 only; `/health` → 200
- Did **not** commit/push; `.env` is gitignored

## Scenarios executed

| SCN-ID | Result | Notes |
|--------|--------|-------|
| SCN-ENV-001 | pass | backend/frontend/ollama tags HTTP ok |
| SCN-ENV-002 | **pass** | generate smoke ok for configured model `qwen2.5-coder:3b` |
| SCN-OFF-001 | pass | pytest exit 0; 390 passed |
| SCN-INV-001 | pass | conformance exit 0; 11 passed |
| SCN-CHAT-001 | **partial / not-run** | L2 started; quality chat smoke exceeded wall-time cap; stopped intentionally |
| SCN-GQ-001 | not-run | IncludeGolden set แต่ไม่ได้ถึงขั้น golden หลัง chat hang |
| SCN-LLM-001 | mitigated | configured model now loads; Explore quality caveat (3b ≪ 14b) |
| SCN-SRC-001 | not-run | Fabric capacity ไม่ active |
| SCN-GATE-001 | human-gate | Trusted / KPI / production — ไม่ approve |

## Failures (sanitized)

- None on L0+L1 after model downsize.
- Prior defect closed: [`defect-handoffs/DEF-001-ollama-oom-configured-model.md`](../defect-handoffs/DEF-001-ollama-oom-configured-model.md)
- Fabric `connected=false` (capacity not active) — human/ops; ไม่ใช่ regression ของ pytest

## Honesty labels

- [x] test-passed (offline)
- [ ] code-complete *(config/.env only — no product code change)*
- [ ] production-verified — **only with live + owner evidence**
- [x] human-gate-pending (Trusted / KPI / Fabric capacity / model quality vs Phase 1 ~14B target)

## Next actions

1. Owner may run **manual Explore** smoke with caveats (3b quality; Postgres/offline when Fabric paused)
2. Optional: free RAM and retry 7b/14b later for better Explore quality
3. Re-attempt L2 with longer budget or GPU when available
4. Commit/push evidence docs when owner asks

## Commit / push

**Not performed by Loop Engineering unless the user explicitly asks.**
