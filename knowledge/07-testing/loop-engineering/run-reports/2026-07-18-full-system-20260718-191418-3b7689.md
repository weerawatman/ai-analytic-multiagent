# Run report — 20260718-191418-3b7689

> **วันที่:** 2026-07-18
> **Scope:** full-system (Loop Engineering QA semi-auto readiness)
> **Level:** 1 (L0+L1); L2 not-run
> **Environment label:** mixed (local services up; Fabric capacity not active; Ollama tags ok / generate OOM on configured model)
> **Raw evidence:** `data/local/qa/loop-engineering/runs/20260718-191418-3b7689/` (gitignored)

## Summary

| Metric | Value |
|--------|-------|
| L0 env smoke | **partial fail** — SCN-ENV-001 pass; SCN-ENV-002 fail (`WebException` / OOM) |
| L1 pytest | **pass** (**390**/390) |
| L1 conformance | **pass** (**11**/11) |
| L2 live / golden | **not-run** (L0 not fully green; chat would risk hang/OOM) |
| Repair rounds used | **1** / 3 (unload + retry 7b — still OOM) |

## Scenarios executed

| SCN-ID | Result | Notes |
|--------|--------|-------|
| SCN-ENV-001 | pass | `start.ps1 -Status`; backend/frontend/ollama tags HTTP ok |
| SCN-ENV-002 | **fail** | generate สำหรับโมเดลที่ตั้งค่า → OOM (`WebException`) |
| SCN-OFF-001 | pass | pytest exit 0; 390 passed |
| SCN-INV-001 | pass | conformance exit 0; 11 passed — ไม่ได้ weaken |
| SCN-CHAT-001 | not-run | L2 ข้าม: L0 generate ไม่ผ่าน + chat CPU อาจเกิน ~10 นาที |
| SCN-LLM-001 | triage-only | จัดเป็น `llm` ตาม policy; ไม่รัน Explore job เต็ม |
| SCN-SRC-001 | not-run | L2; Fabric capacity ไม่ active (human/ops) |
| SCN-GQ-001 | not-run | L2 ข้ามตามเกณฑ์ L0 |
| SCN-GATE-001 | human-gate | Trusted / KPI / production — ไม่ approve |

## Failures (sanitized)

1. **SCN-ENV-002** — Category: `llm`
   - Exception type: `WebException` (HTTP 500)
   - Root cause class: out-of-memory ตอนโหลด `qwen2.5-coder:7b`
   - Defect: [`defect-handoffs/DEF-001-ollama-oom-configured-model.md`](../defect-handoffs/DEF-001-ollama-oom-configured-model.md)
2. **Fabric connected=false** — Category: `human-gate` / ops (capacity not active) — ไม่ใช่ regression ของ pytest; บล็อก live SQL บน Fabric

## Honesty labels

- [x] test-passed (offline)
- [ ] code-complete *(ไม่มีการแก้โค้ดในรอบนี้)*
- [ ] production-verified — **only with live + owner evidence**
- [x] human-gate-pending (Trusted / KPI / Fabric capacity / model RAM)

## Next actions

1. Owner: เพิ่ม RAM ว่างแล้ว retry 7b/14b — หรือยอมรับโมเดลเล็กลงชั่วคราวใน `.env`
2. หลัง SCN-ENV-002 เขียว ค่อยพิจารณา L2 (`-SkipLive` หรือ chat แบบมี time box)
3. ไม่ commit/push จากรอบ QA นี้

## Commit / push

**Not performed by Loop Engineering unless the user explicitly asks.**
